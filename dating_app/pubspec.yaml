name: dating_app
description: LoveMatch dating app MVP
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.9.0
  geolocator: ^14.0.3
  in_app_purchase: ^3.3.0
  firebase_storage: ^13.5.0
  image_picker: ^1.2.3
  emoji_picker_flutter: ^4.5.3
  firebase_messaging: ^16.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profiles/
